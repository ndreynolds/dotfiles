#!/usr/bin/bash

ssh ndreynolds@ndreynolds.com ls /home/git/ > /tmp/REPO_NAMES

file=/tmp/REPO_NAMES
cnt=0
cat $file | while read line
do
    let cnt++
    short=${line:0:(-4)}
    if [ ! -d $short ]; then
        git clone --recursive git@ndreynolds.com:$line
    fi
done
